## What and why

## How I tested it

- [ ] `go test ./...` and `go vet ./...` pass
- [ ] Anything that writes to Woffu still asks for confirmation
- [ ] UI changes checked with the preview harness (`WOFFUX_TUI_PREVIEW`)
